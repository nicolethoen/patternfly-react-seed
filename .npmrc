# Optimize npm for CI/CD environments
# Reduce memory usage during install
fund=false
audit=false
progress=false

# Use cache to speed up installs
prefer-offline=true

# Optimize package resolution
package-lock=true

